name: CI

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  test-job:
    name: Test job
    runs-on: ubuntu-latest

    steps:
      - name: Baixar o código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Executar teste
        run: echo "This job tests something"

    test-job2:
      name: test job 2
      run: echo "algum comentario"
